// string

console.log("hello world");

const value = "lokesh";
const value2 = "vasavi";
const value3 = "both are best friends";
console.log(value+" " + value2 + " " + value3);


const n1=8;
const n2 = "lokesh"
if(n1 === 8)
{
    const n1=9;
    {
        if(n1 ===9)
        {
            console.log(n1);
        }
    }
}
else{
    console.log(n2)
}


const abc = " LOKesh0 abc  ";
console.log(abc.length);   // to get the lenght of any variable 
console.log(abc.toLowerCase());  // to convert any string to lowercase
console.log(abc.toUpperCase());  // to convert any string to upper case 
console.log(abc.trim());  // to trim or remove the whitespace of begining and ending of string

// lets  start javascript : javascript helps to cfreate functionaility

// string  : string are  values made up of text and can contain letters, numbers, symbols, punctuation, and even emojis! 
// number  : Numbers are values that can be used in mathematical operations
// boolen  : boolean value is one that can either be TRUE or FALSE. for example yes or no , on or off , true or false
// opreaters : Operators are the symbols between values that allow different operations like addition, subtraction, multiplication, and more.
// variables : Variables are named values and can store any type of JavaScript value.
// functions : JavaScript functions are reusable blocks of code that perform a specific task, taking some form of input and returning an output.
// conditions : Conditional statements control behavior in JavaScript and determine whether or not pieces of code can run.
// array  : to Store set of values with same data type
// objects : to store set of values with diffrent data type

// variables  : 
// there are 4 ways to declare the variables 
// 1 . var
// 2 . let 
// 3 . const 
// 4 . nothing
// variables are to storing the data.
// for example : x,y,z are three varibles 
var x = 10;
var y = 15;
var z = x + y;
console.log(z);
// here let and const are block scoped 
// here var is not scoped block 

// const value cannot change 
// let value can change after declaring the value before 







