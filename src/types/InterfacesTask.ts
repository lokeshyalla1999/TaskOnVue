import {ILinks} from "../types/DrawerPage"
import { IColumns , IOptions , IRows , Ilabeloptions} from "./InterfacesAll"
export interface AllInterFaces{
      links : ILinks[],
      columns : IColumns[],
      options : IOptions[],
      rows : IRows[],
      Gender : Ilabeloptions[]
}